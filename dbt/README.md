# dbt

Placeholder for future dbt models. The project currently does its
transformations in plain SQL/Python (see `database/` and `scrapers/`);
this folder is reserved so a dbt project can be dropped in later
without another restructure, per the modernization proposal's
folder-structure spec.
